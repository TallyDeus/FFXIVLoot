# Copy to `.env.local` for local dev (Create React App loads `.env.local` automatically).
# Production: use `.env.production` or your host’s env (e.g. GitHub Actions secret).

REACT_APP_API_URL=http://localhost:5000
